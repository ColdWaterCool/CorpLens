# 输出成品结构（固定）

生成结果必须包含以下一级标题，顺序固定：

1. `# {企业名称} · 转型分析草案`（文档总标题）
2. `## 1. 现状分析`
3. `## 2. 转型阶段判断`（含当前阶段 L0–L4）
4. `## 3. 方向建议（含盈利/价值抓手）`
5. `## 4. 近期行动`
6. `## 5. 说明`

解析器按 `## 1.` … `## 5.` 切段展示；缺段时界面会提示。
